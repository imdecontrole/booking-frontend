{
  "name": "booking-frontend",
  "version": "1.0.0",
  "scripts": {
    "start": "npx serve -s . -l 3000",
    "build": "echo 'Static site - no build needed' && cp -r . /app/build"
  },
  "dependencies": {
    "serve": "^14.0.0"
  }
}
